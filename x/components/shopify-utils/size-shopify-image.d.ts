export declare function parseImageSize(spec: string): {
    width: number;
    height: number;
};
export declare function sizeShopifyImage(url: string, size: undefined | {
    width: number;
    height: number;
}): string;
