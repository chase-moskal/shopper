import { Source } from "../../types.js";
export declare function runLeadingWhitespaceRegex({ source, index, setIndex }: {
    source: Source;
    index: number;
    setIndex: (newIndex: number) => void;
}): string;
