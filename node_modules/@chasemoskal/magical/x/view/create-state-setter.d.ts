import { StateMap, StateSetter } from "./types.js";
export declare function createStateSetter<xValue>({ stateMap, stateIndex, rerender, }: {
    stateMap: StateMap;
    stateIndex: number;
    rerender: () => void;
}): StateSetter<xValue>;
