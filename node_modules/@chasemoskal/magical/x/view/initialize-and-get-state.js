export function initializeAndGetState({ initial, stateIndex, stateMap, }) {
    const initialized = stateMap.has(stateIndex);
    if (!initialized)
        stateMap.set(stateIndex, [
            (typeof initial === "function")
                ? initial()
                : initial,
            undefined,
        ]);
    return stateMap.get(stateIndex);
}
//# sourceMappingURL=initialize-and-get-state.js.map