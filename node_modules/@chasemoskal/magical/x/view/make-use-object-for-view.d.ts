import { UseView } from "./types.js";
export declare function makeUseObjectForView({ stateMap, setupMap, render, getRenderCount, pullStateIndex, pullSetupIndex, }: {
    stateMap: Map<number, [any, any]>;
    setupMap: Map<number, () => void>;
    render(): void;
    getRenderCount(): number;
    pullStateIndex(): number;
    pullSetupIndex(): number;
}): UseView;
