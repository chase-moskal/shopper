import { api, service } from "../../service-and-api.js";
export const clientsideApi = api({
    clientService: service()
        .policy(async () => { })
        .expose(() => ({
        async getClientTime() {
            return Date.now();
        },
    })),
});
export const makeServersideApi = (controls, clientside) => (api({
    serverService: service()
        .policy(async () => { })
        .expose(() => ({
        async getServerTime() {
            return Date.now();
        },
        async promptToAskBack() {
            return clientside.clientService.getClientTime();
        },
    })),
}));
//# sourceMappingURL=example-socket-apis.js.map