import { Api, MetaMap } from "../types.js";
export declare function webSocketClient<xServerApi extends Api>({ link, timeout, metaMap, clientApi, handleConnectionClosed, }: {
    link: string;
    timeout: number;
    metaMap: MetaMap<xServerApi>;
    clientApi: Api;
    handleConnectionClosed(): void;
}): Promise<{
    remote: import("../types.js").ApiRemote<xServerApi>;
    close: () => void;
}>;
