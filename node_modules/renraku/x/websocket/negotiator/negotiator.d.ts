import { HttpHeaders, JsonRpcRequestWithMeta, JsonRpcResponse, Logger, Servelet } from "../../types.js";
export declare function negotiator({ logger, timeout, exposeErrors }: {
    logger: Logger;
    timeout: number;
    exposeErrors: boolean;
}): {
    startWaitingForResponse: () => {
        id: number;
        response: Promise<any>;
    };
    acceptIncoming({ servelet, headers, incoming, respond }: {
        servelet: Servelet;
        headers: HttpHeaders;
        incoming: JsonRpcRequestWithMeta | JsonRpcResponse;
        respond: (response: JsonRpcResponse) => void;
    }): Promise<void>;
};
