export declare class Id {
    #private;
    static fromBinary(binary: ArrayBuffer): Id;
    static fromString(text: string): Id;
    static isId(text: string): boolean;
    constructor(binary: ArrayBuffer);
    get binary(): ArrayBuffer;
    get string(): string;
    toBinary(): ArrayBuffer;
    toString(): string;
    equals(id: Id | string): boolean;
}
