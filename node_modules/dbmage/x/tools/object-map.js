export function objectMap(input, mapper) {
    const output = {};
    for (const [key, value] of Object.entries(input))
        output[key] = mapper(value, key);
    return output;
}
export function objectMap2(input, mapper) {
    const output = {};
    for (const [key, value] of Object.entries(input))
        output[key] = mapper(value, key);
    return output;
}
export function objectMap3(input, mapper) {
    return (Object.fromEntries(Object.entries(input).map(([key, value]) => [key, mapper(value, key)])));
}
export function objectTransform(input, mapper) {
    return (Object.fromEntries(Object.entries(input).map(mapper)));
}
//# sourceMappingURL=object-map.js.map