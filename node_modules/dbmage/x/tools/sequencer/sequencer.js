export function sequencer(fun) {
    let promiseChain = Promise.resolve();
    return (async (...args) => {
        const next = promiseChain.then(() => fun(...args));
        promiseChain = next.catch(() => { });
        return next;
    });
}
//# sourceMappingURL=sequencer.js.map