import { Database, FlexStorage, Schema, SchemaToShape } from "../types.js";
export declare function flex<xSchema extends Schema>({ shape, flexStorage, makeTableName, }: {
    flexStorage: FlexStorage;
    shape: SchemaToShape<xSchema>;
    makeTableName?: (path: string[]) => string;
}): Database<xSchema>;
