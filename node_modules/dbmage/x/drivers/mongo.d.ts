import { MongoClient, TransactionOptions } from "mongodb";
import { MongoDatabase, Schema, SchemaToShape } from "../types.js";
export declare function mongo<xSchema extends Schema>({ dbName, client, shape, transactionOptions, makeTableName, }: {
    dbName: string;
    client: MongoClient;
    shape: SchemaToShape<xSchema>;
    transactionOptions?: TransactionOptions;
    makeTableName?: (path: string[]) => string;
}): MongoDatabase<xSchema>;
