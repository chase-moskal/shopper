import { Schema, SchemaToShape } from "../types.js";
export declare function file<xSchema extends Schema>({ path, shape, makeTableName, }: {
    path: string;
    shape: SchemaToShape<xSchema>;
    makeTableName?: (path: string[]) => string;
}): import("../types.js").Database<xSchema>;
