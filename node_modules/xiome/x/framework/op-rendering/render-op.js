import { whenOpReady } from "./when-op-ready.js";
import { html } from "../component.js";
export function renderOp(op, render, more = null, { loadingMessage = "", errorMessage = "", hideErrorText = false } = {}) {
    return html `
		<xio-op
			.op=${op}
			loading-message="${loadingMessage}"
			error-message="${errorMessage}"
			?hide-error-text=${hideErrorText}>
				${whenOpReady(op, render)}
				${more}
		</xio-op>
	`;
}
//# sourceMappingURL=render-op.js.map