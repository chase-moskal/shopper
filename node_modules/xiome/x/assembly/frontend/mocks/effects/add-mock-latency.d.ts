import * as renraku from "renraku";
export interface MockLatencyRange {
    min: number;
    max: number;
}
export type MockLatency = false | MockLatencyRange;
export declare function addMockLatency<xApi extends renraku.Api>({ latency, remote }: {
    latency: MockLatency;
    remote: renraku.Remote<xApi>;
}): renraku.Remote<xApi>;
