import { SecretConfig } from "../types/secret-config.js";
export declare const mockConfig: ({ root, platformHome, platformOrigins }: {
    root: string;
    platformHome: string;
    platformOrigins: string[];
}) => SecretConfig;
