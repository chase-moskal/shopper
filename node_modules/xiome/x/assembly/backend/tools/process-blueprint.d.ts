import { Blueprint } from "../types/blueprint.js";
type ProcessedBlueprint<xBlueprint extends Blueprint, xValue> = {
    [P in keyof xBlueprint]: xBlueprint[P] extends true ? xValue : xBlueprint[P] extends Blueprint ? ProcessedBlueprint<xBlueprint[P], xValue> : never;
};
export declare function processBlueprint<xBlueprint extends Blueprint, xValue>({ blueprint, process }: {
    blueprint: xBlueprint;
    process: (path: string[]) => xValue;
}): ProcessedBlueprint<xBlueprint, xValue>;
export {};
