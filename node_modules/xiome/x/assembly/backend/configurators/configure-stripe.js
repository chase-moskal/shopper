import { realStripeCircuit } from "../../../features/store/backend/stripe/real-stripe-circuit.js";
export async function configureStripe({ config, storeDatabaseRaw, prepareRoleManager, }) {
    return realStripeCircuit({
        logger: console,
        storeDatabaseRaw,
        prepareRoleManager,
        stripeConfig: config.stripe,
    });
}
//# sourceMappingURL=configure-stripe.js.map