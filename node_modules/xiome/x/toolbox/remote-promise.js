export function remotePromise() {
    let resolve;
    let reject;
    const promise = new Promise((resolve2, reject2) => {
        reject = reject2;
        resolve = resolve2;
    });
    return { promise, resolve, reject };
}
//# sourceMappingURL=remote-promise.js.map