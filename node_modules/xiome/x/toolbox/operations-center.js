export function makeOperationsCenter() {
    let count = 0;
    const never = new Promise(() => { });
    return {
        async run(op) {
            count += 1;
            const remember = count;
            const result = await op;
            return count === remember
                ? result
                : never;
        },
    };
}
//# sourceMappingURL=operations-center.js.map