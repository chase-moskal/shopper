export function makeQueryString(query) {
    const entries = Object.entries(query);
    let queryString = entries.length
        ? "?"
        : "";
    queryString += entries
        .map(([key, data]) => `${key}=${data}`)
        .join("&");
    return queryString;
}
//# sourceMappingURL=make-query-string.js.map