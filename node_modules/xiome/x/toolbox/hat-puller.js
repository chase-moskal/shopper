export function hatPuller(items) {
    let hat = [];
    return function pick() {
        if (hat.length === 0)
            hat = [...items];
        const index = Math.floor(Math.random() * hat.length);
        const item = hat[index];
        hat.splice(index, 1);
        return item;
    };
}
//# sourceMappingURL=hat-puller.js.map