export declare class AssertiveMap<Key, Value> extends Map<Key, Value> {
    #private;
    constructor(makeValue: (key: Key) => Value, entries?: readonly (readonly [Key, Value])[] | null);
    assert(key: Key): Value;
}
