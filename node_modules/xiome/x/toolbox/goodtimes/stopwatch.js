export async function stopwatch(promise) {
    const start = Date.now();
    const result = await promise;
    const time = Date.now() - start;
    return [time, result];
}
//# sourceMappingURL=stopwatch.js.map