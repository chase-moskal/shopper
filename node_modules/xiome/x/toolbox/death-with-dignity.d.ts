import { Logger } from "./logger/interfaces.js";
export declare function deathWithDignity({ logger }?: {
    logger?: Logger;
}): {
    onDeath: (...listeners: (() => void | Promise<void>)[]) => () => void;
};
