export function validateResponse({ event, namespace, popupOrigin, }) {
    const relevant = !!event.data
        && event.data.namespace === namespace;
    const originsMatch = event.origin.toLowerCase() === popupOrigin.toLowerCase();
    if (relevant) {
        if (originsMatch)
            return true;
        else
            console.warn(`message denied from origin "${event.origin}"`, event);
    }
    return false;
}
//# sourceMappingURL=validate-response.js.map