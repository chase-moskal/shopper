export declare function setupSimplePopup<Parameters, Payload>({ namespace, allowedOrigins, action, }: {
    namespace: string;
    allowedOrigins: string[];
    action: (parameters: Parameters) => Promise<Payload>;
}): void;
