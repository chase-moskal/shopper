import { WebsiteContext } from "./build-website-types.js";
export declare function buildWebsite<xOptions extends WebsiteContext>({ inputDirectory, outputDirectory, context, excludes, logWrittenFile, }: {
    inputDirectory: string;
    outputDirectory: string;
    context: Omit<xOptions, keyof WebsiteContext>;
    excludes: string[];
    logWrittenFile?(path: string): void;
}): Promise<void>;
