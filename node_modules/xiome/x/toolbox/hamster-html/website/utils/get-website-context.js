import { dirname, relative, resolve } from "path";
import { hashVersioner } from "../../versioning/hash-versioner.js";
export function getWebsiteContext({ sourcePath, inputDirectory, outputDirectory, }) {
    let base = relative(dirname(sourcePath), inputDirectory);
    base = base === ""
        ? "."
        : base;
    return {
        base,
        v: hashVersioner({
            root: outputDirectory,
            origin: relative(resolve(inputDirectory), dirname(resolve(sourcePath))),
        })
    };
}
//# sourceMappingURL=get-website-context.js.map