export interface HashVersioner {
    (url: string): Promise<string>;
}
export declare function hashVersioner({ root, origin }: {
    root: string;
    origin?: string;
}): HashVersioner;
export declare class HashVersionerError extends Error {
    name: string;
    constructor(message: string);
}
