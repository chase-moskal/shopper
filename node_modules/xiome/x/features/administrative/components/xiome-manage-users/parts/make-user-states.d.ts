import { UserState2 } from "../types/user-state.js";
import { Op } from "../../../../../framework/ops.js";
import { UserResult } from "../../../api/types/user-result.js";
export declare function makeUserStates({ getUserResultsOp, rerender, }: {
    getUserResultsOp: () => Op<UserResult[]>;
    rerender: () => void;
}): {
    cleanupObsoleteStates: () => void;
    obtainStateForUser: (userId: string) => UserState2;
};
