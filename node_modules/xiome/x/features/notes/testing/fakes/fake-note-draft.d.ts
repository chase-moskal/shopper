import { DraftForNote, Notes } from "../../types/notes-concepts.js";
export declare function fakeNoteDraft(userId: string, overrides?: Partial<DraftForNote<Notes.Message>>): DraftForNote<Notes.Message>;
