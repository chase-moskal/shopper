import { fakeNoteDraft } from "./fake-note-draft.js";
export function fakeManyNoteDrafts(userId, n) {
    const noteDrafts = [];
    for (let i = 0; i < n; i++) {
        noteDrafts.push(fakeNoteDraft(userId, {
            title: `fake note number ${i}`,
        }));
    }
    return noteDrafts;
}
//# sourceMappingURL=fake-many-note-drafts.js.map