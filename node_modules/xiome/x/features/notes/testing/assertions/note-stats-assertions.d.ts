import { makeNotesModel } from "../../models/notes-model.js";
export declare function prepareNoteStatsAssertions({ notesModel }: {
    notesModel: ReturnType<typeof makeNotesModel>;
}): {
    assertNoteCounts({ newCount, oldCount }: {
        newCount: number;
        oldCount: number;
    }): void;
};
