import { Rando } from "dbmage";
import { DatabaseSafe } from "../../../assembly/backend/types/database.js";
import { DraftForNote, Notes } from "../types/notes-concepts.js";
export declare function makeNotesDepositBox({ rando, database }: {
    rando: Rando;
    database: DatabaseSafe;
}): {
    sendNotes: (drafts: DraftForNote<Notes.Any>[]) => Promise<{
        noteId: string;
    }[]>;
    sendNote: (draft: DraftForNote<Notes.Any>) => Promise<{
        noteId: string;
    }>;
};
