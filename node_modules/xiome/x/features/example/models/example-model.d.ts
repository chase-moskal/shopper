import { Op } from "../../../framework/ops.js";
import { AccessPayload } from "../../auth/types/auth-tokens.js";
export declare function makeExampleModel({ getAccessOp }: {
    getAccessOp: () => Op<AccessPayload>;
}): {
    state: {
        accessOp: Op<AccessPayload>;
    };
    subscribe: (subscription: import("@chasemoskal/snapstate").Subscription<{
        accessOp: Op<AccessPayload>;
    }>) => () => void;
    updateAccessOp: (op: Op<AccessPayload>) => void;
};
