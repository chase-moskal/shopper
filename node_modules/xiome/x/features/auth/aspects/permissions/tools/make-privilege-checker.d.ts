import { Permit } from "../types/permit.js";
import { PrivilegeChecker } from "../types/privilege-checker.js";
export declare function makePrivilegeChecker<xPrivileges extends {
    [key: string]: string;
}>(permit: Permit, privileges: xPrivileges): PrivilegeChecker<xPrivileges>;
