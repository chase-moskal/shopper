import * as renraku from "renraku";
import { AuthOptions } from "../../../types/auth-options.js";
import { ProfileDraft } from "../routines/personal/types/profile-draft.js";
export declare const makePersonalService: (options: AuthOptions) => renraku.Service<import("../../../types/auth-metas.js").UserMeta, import("../../../types/auth-metas.js").UserAuth, {
    setProfile({ userId: userIdString, profileDraft }: {
        userId: string;
        profileDraft: ProfileDraft;
    }): Promise<void>;
}>;
