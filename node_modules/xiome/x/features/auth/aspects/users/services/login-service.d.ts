import * as renraku from "renraku";
import { AuthOptions } from "../../../types/auth-options.js";
export declare const makeLoginService: ({ rando, config, authPolicies, signToken, verifyToken, sendLoginEmail, generateNickname, }: AuthOptions) => renraku.Service<import("../../../types/auth-metas.js").AnonMeta, import("../../../types/auth-metas.js").LoginAuth, {
    sendLoginLink(inputs: {
        email: string;
    }): Promise<void>;
    authenticateViaLoginToken({ loginToken }: {
        loginToken: string;
    }): Promise<import("../../../../../toolbox/concurrent.js").AwaitProps<{
        accessToken: Promise<string>;
        refreshToken: Promise<string>;
    }>>;
}>;
