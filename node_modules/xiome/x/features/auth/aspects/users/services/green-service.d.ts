import * as renraku from "renraku";
import { AuthOptions } from "../../../types/auth-options.js";
import { Scope } from "../../../types/auth-tokens.js";
export declare const makeGreenService: (options: AuthOptions) => renraku.Service<undefined, import("../../../types/auth-metas.js").GreenAuth, {
    authorize({ scope, refreshToken, appId: appIdString, }: {
        scope: Scope;
        appId: string;
        refreshToken?: string;
    }): Promise<string>;
}>;
