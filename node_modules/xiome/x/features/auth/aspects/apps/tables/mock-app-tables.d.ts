import { FlexStorage } from "dbmage";
export declare function mockAppTables(storage: FlexStorage): Promise<import("dbmage").SchemaToTables<{
    registrations: {
        appId: import("dbmage").Id;
        label: string;
        home: string;
        origins: string;
        archived: boolean;
    };
    owners: {
        appId: import("dbmage").Id;
        userId: import("dbmage").Id;
    };
}>>;
