import * as renraku from "renraku";
import { AppDraft } from "../types/app-draft.js";
import { AuthOptions } from "../../../types/auth-options.js";
import { AdminEmailDisplay } from "../types/admin-email-display.js";
export declare const makeAppEditService: ({ rando, config, authPolicies, generateNickname, }: AuthOptions) => renraku.Service<import("../../../types/auth-metas.js").AppOwnerMeta, import("../../../types/auth-metas.js").AppOwnerAuth, {
    updateApp({ appId: appIdString, appDraft }: {
        appId: string;
        appDraft: AppDraft;
    }): Promise<void>;
    deleteApp({ appId: appIdString }: {
        appId: string;
    }): Promise<void>;
    listAdmins({ appId: appIdString }: {
        appId: string;
    }): Promise<AdminEmailDisplay[]>;
    assignPlatformUserAsAdmin({ appId: appIdString, platformUserId: platformUserIdString }: {
        appId: string;
        platformUserId: string;
    }): Promise<void>;
    assignAdmin({ appId: appIdString, email }: {
        appId: string;
        email: string;
    }): Promise<void>;
    revokeAdmin({ appId: appIdString, userId: userIdString }: {
        appId: string;
        userId: string;
    }): Promise<void>;
}>;
