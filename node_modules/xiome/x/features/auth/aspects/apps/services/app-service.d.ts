import * as renraku from "renraku";
import { AppDraft } from "../types/app-draft.js";
import { AppDisplay } from "../types/app-display.js";
import { AuthOptions } from "../../../types/auth-options.js";
export declare const makeAppService: ({ rando, config, authPolicies, }: AuthOptions) => renraku.Service<import("../../../types/auth-metas.js").PlatformUserMeta, import("../../../types/auth-metas.js").PlatformUserAuth, {
    listApps({ ownerUserId: ownerUserIdString }: {
        ownerUserId: string;
    }): Promise<AppDisplay[]>;
    registerApp({ appDraft, ownerUserId }: {
        appDraft: AppDraft;
        ownerUserId: string;
    }): Promise<AppDisplay>;
}>;
