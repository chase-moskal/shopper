import { AppFormDraft } from "./types/app-form-draft.js";
export declare function makeAppForm({ clearOnSubmit, submitButtonText, showAdditionalOrigins, initialValues, query, submit, requestUpdate, }: {
    clearOnSubmit: boolean;
    submitButtonText: string;
    showAdditionalOrigins: boolean;
    initialValues?: AppFormDraft;
    requestUpdate: () => void;
    submit: (draft: AppFormDraft) => Promise<void>;
    query: <E extends HTMLElement>(selector: string) => E;
}): {
    render: ({ partNamespace }: {
        partNamespace: string;
    }) => import("lit-html").TemplateResult<1>;
};
