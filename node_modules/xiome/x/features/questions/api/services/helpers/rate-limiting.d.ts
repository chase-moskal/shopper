import { Id } from "dbmage";
import { QuestionsDatabase } from "../../types/questions-schema.js";
export declare function rateLimitQuestions({ userId, database }: {
    userId: Id;
    database: QuestionsDatabase;
}): Promise<void>;
export declare function rateLimitAnswers({ userId, questionId, database }: {
    userId: Id;
    questionId: Id;
    database: QuestionsDatabase;
}): Promise<void>;
