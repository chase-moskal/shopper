import * as renraku from "renraku";
import { AnswerDraft } from "../types/answer-draft.js";
import { Answer } from "../types/questions-and-answers.js";
import { UserMeta } from "../../../auth/types/auth-metas.js";
import { QuestionsApiOptions } from "../types/questions-api-options.js";
export declare const makeQuestionsAnsweringService: (options: QuestionsApiOptions) => renraku.Service<UserMeta, import("../types/questions-metas-and-auths.js").QuestionsUserAuth, {
    postAnswer(inputs: {
        questionId: string;
        answerDraft: AnswerDraft;
    }): Promise<Answer>;
    archiveAnswer(inputs: {
        archive: boolean;
        answerId: string;
    }): Promise<void>;
    likeAnswer(inputs: {
        like: boolean;
        answerId: string;
    }): Promise<void>;
    reportAnswer(inputs: {
        report: boolean;
        answerId: string;
    }): Promise<void>;
}>;
