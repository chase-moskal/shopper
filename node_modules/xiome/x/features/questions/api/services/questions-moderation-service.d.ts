import * as renraku from "renraku";
import { UserMeta } from "../../../auth/types/auth-metas.js";
import { QuestionsApiOptions } from "../types/questions-api-options.js";
export declare const makeQuestionsModerationService: (options: QuestionsApiOptions) => renraku.Service<UserMeta, import("../types/questions-metas-and-auths.js").QuestionsUserAuth, {
    archiveBoard({ board }: {
        board: string;
    }): Promise<void>;
    fetchReportedQuestions(): Promise<void>;
}>;
