import * as renraku from "renraku";
import { QuestionDraft } from "../types/question-draft.js";
import { UserMeta } from "../../../auth/types/auth-metas.js";
import { Question } from "../types/questions-and-answers.js";
import { QuestionsApiOptions } from "../types/questions-api-options.js";
export declare const makeQuestionsPostingService: (options: QuestionsApiOptions) => renraku.Service<UserMeta, import("../types/questions-metas-and-auths.js").QuestionsUserAuth, {
    postQuestion(inputs: {
        questionDraft: QuestionDraft;
    }): Promise<Question>;
    archiveQuestion(inputs: {
        archive: boolean;
        questionId: string;
    }): Promise<void>;
    likeQuestion(inputs: {
        like: boolean;
        questionId: string;
    }): Promise<void>;
    reportQuestion(inputs: {
        report: boolean;
        questionId: string;
    }): Promise<void>;
}>;
