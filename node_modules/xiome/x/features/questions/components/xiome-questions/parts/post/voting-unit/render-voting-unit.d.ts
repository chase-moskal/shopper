import { SVGTemplateResult } from "../../../../../../../framework/component.js";
export declare function renderVotingUnit({ title, dataVote, voteCount, voteCasted, icon, castVote, }: {
    title: string;
    dataVote: string;
    voteCount: number;
    voteCasted: boolean;
    icon: SVGTemplateResult;
    castVote?: (status: boolean) => void;
}): import("lit-html").TemplateResult<1>;
