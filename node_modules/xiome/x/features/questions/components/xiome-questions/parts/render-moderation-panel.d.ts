import { ModalSystem } from "../../../../../assembly/frontend/modal/types/modal-system.js";
export declare function renderModerationPanel({ modals, board, archiveBoard }: {
    board: string;
    modals: ModalSystem;
    archiveBoard: () => Promise<void>;
}): import("lit-html").TemplateResult<1>;
