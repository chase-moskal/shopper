import { ChatPost } from "../../../common/types/chat-concepts.js";
export declare function renderChatPost({ post, isModerator, mute, remove, mutedIds, }: {
    post: ChatPost;
    isModerator: boolean;
    mute: () => void;
    remove: () => void;
    mutedIds: string[];
}): import("lit-html").TemplateResult<1>;
