import { ChatAuth, ChatMeta } from "../../common/types/chat-concepts.js";
export declare function mockChatMeta({ access }: ChatAuth): Promise<ChatMeta>;
export declare function mockChatPolicy({ accessToken }: ChatMeta): Promise<ChatAuth>;
