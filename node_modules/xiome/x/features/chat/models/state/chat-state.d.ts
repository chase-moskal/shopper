import { Op } from "../../../../framework/ops.js";
import { AccessPayload } from "../../../auth/types/auth-tokens.js";
import { ChatConnection, ChatPost, ChatStats, ChatStatus } from "../../common/types/chat-concepts.js";
export declare function makeChatState(): import("@chasemoskal/snapstate").Snapstate<{
    accessOp: Op<AccessPayload>;
    connectionOp: Op<ChatConnection>;
    cache: {
        mutedUserIds: string[];
        roomStats: ChatStats;
        rooms: {
            [key: string]: {
                status: ChatStatus;
                posts: ChatPost[];
            };
        };
    };
}>;
