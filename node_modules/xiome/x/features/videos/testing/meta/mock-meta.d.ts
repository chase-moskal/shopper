import { VideoMeta } from "../../types/video-auth.js";
export declare function mockVideoMeta({ appId, userId, origins, privileges }: {
    appId: string;
    userId: string;
    origins: string[];
    privileges: string[];
}): Promise<VideoMeta>;
