import { Dacast } from "../../types/dacast-types.js";
import { QueryData } from "../../../../../toolbox/make-query-string.js";
export declare function dacastRestModern({ apiKey, headers: moreHeaders }: {
    apiKey: string;
    headers: Partial<Dacast.Headers>;
}): {
    get: <xResult, xQuery extends QueryData = QueryData>(url: string, queryDefaults?: QueryData) => (queryAdditions?: xQuery) => Promise<xResult>;
};
