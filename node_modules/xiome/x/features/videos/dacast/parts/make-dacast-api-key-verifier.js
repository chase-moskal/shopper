export function makeDacastApiKeyVerifier(getDacastClient) {
    return async (apiKey) => {
        try {
            const dacast = getDacastClient(apiKey);
            await dacast.vods.get();
            return true;
        }
        catch (error) {
            return false;
        }
    };
}
//# sourceMappingURL=make-dacast-api-key-verifier.js.map