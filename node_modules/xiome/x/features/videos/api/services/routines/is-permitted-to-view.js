export function isPermittedToView({ viewPrivileges, userPrivileges }) {
    for (const requiredPrivilege of viewPrivileges) {
        const userHasPrivilege = userPrivileges
            .find(p => p === requiredPrivilege);
        if (userHasPrivilege)
            return true;
    }
    return false;
}
//# sourceMappingURL=is-permitted-to-view.js.map