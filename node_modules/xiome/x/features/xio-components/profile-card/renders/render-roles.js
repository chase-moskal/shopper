import { html } from "../../../../framework/component.js";
export function renderRoles(user) {
    return html `
		<ul class=roles>
			${user.roles.map(role => html `
				<li
					data-role-label="${role.label}"
					data-role-id="${role.roleId}">
						${role.label}
				</li>
			`)}
		</ul>
	`;
}
//# sourceMappingURL=render-roles.js.map