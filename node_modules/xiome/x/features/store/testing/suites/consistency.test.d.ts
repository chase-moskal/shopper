declare const _default: {
    "the system doesn't explode when": {
        "customer record is deleted"(): Promise<void>;
        "products are deleted"(): Promise<void>;
    };
};
export default _default;
