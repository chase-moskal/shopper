import * as dbmage from "dbmage";
import { StoreDatabase } from "./types/schema.js";
export declare function getConnectedStoreDatabase({ tables, transaction, }: StoreDatabase, connectId: dbmage.Id): StoreDatabase;
export type ConnectConstraint = {
    connectId: dbmage.Id;
};
