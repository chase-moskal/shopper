import * as renraku from "renraku";
import { StoreServiceOptions } from "../types/options.js";
import { StripeConnectStatus } from "../../isomorphic/concepts.js";
export declare const makeConnectService: (options: StoreServiceOptions) => renraku.Service<any, import("../policies/types.js").StoreAuth, {
    loadConnectDetails(): Promise<{
        connectDetails: import("../../isomorphic/concepts.js").StripeConnectDetails;
        connectStatus: StripeConnectStatus;
    }>;
    generatePopupForStripeAccountOnboarding(): Promise<{
        popupId: string;
        stripeAccountId: string;
        stripeAccountSetupLink: string;
    }>;
    generateStripeLoginLink(): Promise<{
        stripeLoginLink: string;
        popupId: string;
    }>;
    pause(): Promise<void>;
    resume(): Promise<void>;
    loadConnectStatus(): Promise<StripeConnectStatus>;
}>;
