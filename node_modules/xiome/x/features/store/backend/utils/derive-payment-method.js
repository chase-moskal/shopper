export function derivePaymentMethod(method) {
    const { card } = method !== null && method !== void 0 ? method : {};
    return card
        ? {
            cardClues: {
                brand: card.brand,
                country: card.country,
                expireMonth: card.exp_month,
                expireYear: card.exp_year,
                last4: card.last4,
            },
        }
        : undefined;
}
//# sourceMappingURL=derive-payment-method.js.map