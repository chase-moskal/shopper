import * as dbmage from "dbmage";
import { StoreDatabase } from "../database/types/schema.js";
import { StripeLiaisonAccount } from "../stripe/liaison/types.js";
export declare function getTierRowByQueryingStripePriceId({ stripePriceId, storeDatabase, stripeLiaisonAccount, }: {
    stripePriceId: string;
    storeDatabase: StoreDatabase;
    stripeLiaisonAccount: StripeLiaisonAccount;
}): Promise<{
    tierId: dbmage.Id;
    stripeProductId: string;
    planId: dbmage.Id;
    roleId: dbmage.Id;
    time: number;
}>;
