import * as dbmage from "dbmage";
import { StoreConnectedAuth } from "../policies/types.js";
export declare function getRowsForTierId({ tierId: tierIdString, auth: { storeDatabase }, }: {
    tierId: string;
    auth: StoreConnectedAuth;
}): Promise<{
    tierRow: {
        tierId: dbmage.Id;
        stripeProductId: string;
        planId: dbmage.Id;
        roleId: dbmage.Id;
        time: number;
    };
    planRow: {
        planId: dbmage.Id;
        label: string;
        time: number;
        archived: boolean;
    };
}>;
