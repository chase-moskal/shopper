export async function updateExistingSubscriptionWithNewTier({ stripePriceId, auth, stripeSubscription, }) {
    const { stripeLiaisonAccount } = auth;
    const previousItemId = stripeSubscription.items.data[0].id;
    const newItems = [
        {
            id: previousItemId,
            price: stripePriceId,
            quantity: 1,
        }
    ];
    return stripeLiaisonAccount.subscriptions.update(stripeSubscription.id, {
        items: newItems,
        cancel_at_period_end: false,
        proration_behavior: "create_prorations",
    });
}
//# sourceMappingURL=update-existing-subscription-with-new-tier.js.map