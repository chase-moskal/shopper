import Stripe from "stripe";
import { StoreCustomerAuth } from "../policies/types.js";
export declare function findStripeSubscriptionRelatedToTier(auth: StoreCustomerAuth, tierId: string): Promise<undefined | Stripe.Subscription>;
