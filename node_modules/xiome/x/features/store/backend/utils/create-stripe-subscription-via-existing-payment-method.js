export async function createStripeSubscriptionViaExistingPaymentMethod(auth, stripePriceId, stripePaymentMethod) {
    return auth.stripeLiaisonAccount.subscriptions.create({
        customer: auth.stripeCustomerId,
        default_payment_method: stripePaymentMethod.id,
        items: [{
                price: stripePriceId,
                quantity: 1,
            }],
    });
}
//# sourceMappingURL=create-stripe-subscription-via-existing-payment-method.js.map