import Stripe from "stripe";
import * as dbmage from "dbmage";
import { StoreDatabase } from "../database/types/schema.js";
export declare function queryTierRowRelatedToStripePrice(storeDatabase: StoreDatabase, price: Stripe.Price): Promise<{
    tierId: dbmage.Id;
    stripeProductId: string;
    planId: dbmage.Id;
    roleId: dbmage.Id;
    time: number;
}>;
