import { getStripeId } from "../stripe/utils/get-stripe-id.js";
export async function queryStripePriceRelatedToSubscription(stripeLiaisonAccount, subscription) {
    const [stripePriceId] = subscription
        .items
        .data
        .map(item => getStripeId(item.price));
    const price = await stripeLiaisonAccount
        .prices
        .retrieve(stripePriceId);
    return price;
}
//# sourceMappingURL=query-stripe-price-related-to-subscription.js.map