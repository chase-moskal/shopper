import * as dbmage from "dbmage";
import { getStripeId } from "../stripe/utils/get-stripe-id.js";
export async function queryTierRowRelatedToStripePrice(storeDatabase, price) {
    return storeDatabase
        .tables
        .subscriptions
        .tiers
        .readOne(dbmage.find({
        stripeProductId: getStripeId(price.product)
    }));
}
//# sourceMappingURL=query-tier-row-related-to-stripe-price.js.map