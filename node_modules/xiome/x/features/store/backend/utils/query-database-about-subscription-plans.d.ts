import * as dbmage from "dbmage";
import { StoreConnectedAuth } from "../policies/types.js";
export declare function queryDatabaseAboutSubscriptionPlans(auth: StoreConnectedAuth): Promise<{
    planRows: {
        planId: dbmage.Id;
        label: string;
        time: number;
        archived: boolean;
    }[];
    tierRows: {
        tierId: dbmage.Id;
        stripeProductId: string;
        planId: dbmage.Id;
        roleId: dbmage.Id;
        time: number;
    }[];
}>;
