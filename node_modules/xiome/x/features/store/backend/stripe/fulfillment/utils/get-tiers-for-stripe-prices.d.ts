import * as dbmage from "dbmage";
import { StripeLiaisonAccount } from "../../liaison/types.js";
import { StoreDatabase } from "../../../database/types/schema.js";
export declare function getTiersForStripePrices({ priceIds, storeDatabase, stripeLiaisonAccount, }: {
    priceIds: string[];
    storeDatabase: StoreDatabase;
    stripeLiaisonAccount: StripeLiaisonAccount;
}): Promise<{
    tierId: dbmage.Id;
    stripeProductId: string;
    planId: dbmage.Id;
    roleId: dbmage.Id;
    time: number;
}[]>;
