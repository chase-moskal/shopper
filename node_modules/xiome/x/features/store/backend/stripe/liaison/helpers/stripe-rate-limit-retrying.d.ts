export declare class RetryFailedError extends Error {
}
export declare function setupStripeRateLimitRetrying({ maxRetries, maxWaitPeriod, }: {
    maxRetries: number;
    maxWaitPeriod: number;
}): <xFuncs extends {
    [key: string]: (...args: any[]) => Promise<any>;
}>(funcs: xFuncs) => { [P in keyof xFuncs]: (...args: any[]) => any; };
