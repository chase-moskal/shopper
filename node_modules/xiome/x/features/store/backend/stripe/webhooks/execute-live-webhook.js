export async function executeLiveWebhook({ stripe, payload, signature, endpointSecret, webhooks, }) {
    const event = await stripe.webhooks.constructEventAsync(payload, signature, endpointSecret);
    const webhook = webhooks[event.type];
    if (webhook)
        await webhook(event);
}
//# sourceMappingURL=execute-live-webhook.js.map