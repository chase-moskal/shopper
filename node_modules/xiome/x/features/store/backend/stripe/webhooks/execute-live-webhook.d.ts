import { Stripe } from "stripe";
import { StripeWebhooks } from "../types.js";
export declare function executeLiveWebhook({ stripe, payload, signature, endpointSecret, webhooks, }: {
    stripe: Stripe;
    payload: any;
    signature: string;
    endpointSecret: string;
    webhooks: StripeWebhooks;
}): Promise<void>;
