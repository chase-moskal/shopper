export const stripeClientReferenceId = {
    build({ appId, userId }) {
        return `${appId} ${userId}`;
    },
    parse(client_reference_id) {
        const [appId, userId] = client_reference_id.split(" ");
        return { appId, userId };
    }
};
//# sourceMappingURL=stripe-client-reference-id.js.map