export declare const stripeClientReferenceId: {
    build({ appId, userId }: {
        appId: string;
        userId: string;
    }): string;
    parse(client_reference_id: string): {
        appId: string;
        userId: string;
    };
};
