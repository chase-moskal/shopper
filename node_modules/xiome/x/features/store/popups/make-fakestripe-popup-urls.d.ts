import { Querystrings } from "./types.js";
export declare function makeFakestripePopupUrls(base: string): {
    connect(params: Querystrings.Connect): string;
    login(): string;
    customerPortal(): string;
    checkout(params: Querystrings.Checkout): string;
};
