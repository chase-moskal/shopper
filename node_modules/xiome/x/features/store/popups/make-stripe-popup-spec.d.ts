import { Popups } from "./types.js";
export declare namespace makeStripePopupSpec {
    function connect({ popupReturnUrl, generateId }: Popups.SpecParams): {
        popupId: string;
        return_url: string;
        refresh_url: string;
    };
    function login({ generateId }: Popups.SpecParams): {
        popupId: string;
    };
    function openCustomerPortal({ generateId, popupReturnUrl }: Popups.SpecParams): {
        popupId: string;
        return_url: string;
    };
    function checkout({ popupReturnUrl, generateId }: Popups.SpecParams): {
        popupId: string;
        success_url: string;
        cancel_url: string;
    };
}
