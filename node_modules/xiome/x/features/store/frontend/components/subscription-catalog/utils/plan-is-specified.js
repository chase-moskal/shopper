export function planIsSpecified(specifiedPlans) {
    return (plan) => (specifiedPlans.length > 0
        ? specifiedPlans.includes(plan.planId)
        : true);
}
//# sourceMappingURL=plan-is-specified.js.map