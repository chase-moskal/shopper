export function getOngoingSubscriptions({ plans = [], mySubscriptionDetails = [], }) {
    return mySubscriptionDetails.map(subscription => {
        const plan = plans
            .find(p => p.planId === subscription.planId);
        const tier = plan
            .tiers
            .find(t => t.tierId === subscription.tierId);
        return {
            plan,
            tier,
            subscription,
            pricing: subscription.pricing,
        };
    });
}
//# sourceMappingURL=get-ongoing-subscriptions.js.map