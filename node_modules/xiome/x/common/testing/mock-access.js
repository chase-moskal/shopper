export function mockAccess({ rando, appId, appOrigin, privileges, }) {
    const userId = rando.randomId().toString();
    return {
        appId: appId.toString(),
        origins: [appOrigin],
        permit: { privileges },
        scope: { core: true },
        user: {
            userId: rando.randomId().toString(),
            profile: {
                avatar: undefined,
                nickname: `user ${userId.slice(0, 6)}`,
                tagline: "",
            },
            roles: [],
            stats: { joined: Date.now() },
        },
    };
}
//# sourceMappingURL=mock-access.js.map