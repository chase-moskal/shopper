/// <reference types="node" />
import { RequestListener } from "http";
import { Api, Logger } from "../types.js";
export declare function nodeServer({ api, exposeErrors, maxPayloadSize, logger, processListener, }: {
    api: Api;
    logger?: Logger;
    exposeErrors: boolean;
    maxPayloadSize: number;
    processListener?: (listener: RequestListener) => RequestListener;
}): import("http").Server<typeof import("http").IncomingMessage, typeof import("http").ServerResponse>;
