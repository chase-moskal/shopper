/// <reference types="node" />
import { ServerResponse } from "http";
export declare function respondWithError({ id, error, res, exposeErrors, }: {
    id: number;
    error: Error;
    res: ServerResponse;
    exposeErrors: boolean;
}): void;
