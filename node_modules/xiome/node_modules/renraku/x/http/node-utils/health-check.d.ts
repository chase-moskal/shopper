/// <reference types="node" />
import { RequestListener } from "http";
import { Logger } from "../../types.js";
export declare function healthCheck(path: string, logger: Logger, listener: RequestListener): RequestListener;
