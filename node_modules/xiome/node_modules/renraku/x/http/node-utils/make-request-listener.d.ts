/// <reference types="node" />
import { RequestListener } from "http";
import { Logger, Servelet } from "../../types.js";
export declare function makeRequestListener({ exposeErrors, maxPayloadSize, logger, execute, }: {
    exposeErrors: boolean;
    maxPayloadSize: number;
    logger?: Logger;
    execute: Servelet;
}): RequestListener;
