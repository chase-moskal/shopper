import { is_service } from "./types.js";
export const service = () => ({
    policy(p) {
        return {
            expose(e) {
                return {
                    [is_service]: is_service,
                    expose: e,
                    policy: p,
                };
            },
        };
    },
});
export function api(api) {
    return api;
}
//# sourceMappingURL=service-and-api.js.map