import { HttpHeaders, JsonRpcRequestWithMeta, JsonRpcResponse, Logger, Servelet } from "../../../types.js";
export declare function acceptIncomingRequest({ logger, exposeErrors, headers, request, servelet, respond, }: {
    logger: Logger;
    headers: HttpHeaders;
    exposeErrors: boolean;
    request: JsonRpcRequestWithMeta;
    servelet: Servelet;
    respond: (response: JsonRpcResponse) => void;
}): Promise<void>;
