import { responseWaiter } from "./response-waiter.js";
import { JsonRpcResponse, Logger } from "../../../types.js";
export declare function acceptIncomingResponse({ logger, response, waiter }: {
    logger: Logger;
    response: JsonRpcResponse;
    waiter: ReturnType<typeof responseWaiter>;
}): Promise<void>;
