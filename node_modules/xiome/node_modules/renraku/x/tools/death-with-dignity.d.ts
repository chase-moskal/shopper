import { Logger } from "../types.js";
export type Signal = "SIGINT" | "SIGTERM" | "uncaughtException" | "unhandledRejection";
export declare function deathWithDignity({ logger }?: {
    logger?: Logger;
}): {
    onDeath: (listener: (signal: Signal) => void) => () => boolean;
};
