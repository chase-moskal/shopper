import { Suite } from "cynic";
export declare const prepareTokenTestingSuite: ({ payload, publicKey, privateKey, }: {
    payload: {
        a: boolean;
        b: number;
    };
    publicKey: string;
    privateKey: string;
}) => Suite;
