import { tokenSettings } from "./internals/token-settings.js";
import * as jwt from "./internals/badmodules/jsonwebtoken.js";
export const tokenSign = async function ({ payload, lifespan, privateKey, }) {
    const data = { payload };
    const signOptions = {
        algorithm: tokenSettings.algorithm,
        expiresIn: lifespan / 1000,
    };
    return new Promise((resolve, reject) => {
        jwt.sign(data, privateKey, signOptions, (error, token) => {
            if (error)
                reject(error);
            else
                resolve(token);
        });
    });
};
//# sourceMappingURL=token-sign.js.map