import { createSign } from "crypto";
import { defaultSignatureSettings } from "./internals/default-signature-settings.js";
export function signatureSign(options) {
    const { body, format, algorithm, privateKey, } = { ...defaultSignatureSettings, ...options };
    const signer = createSign(algorithm);
    signer.write(body);
    signer.end();
    return signer.sign(privateKey, format);
}
//# sourceMappingURL=signature-sign.js.map